<UserControl x:Class="PCInfoApp.BenchmarkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Công cụ Benchmark" Style="{StaticResource H2Style}" Margin="0,0,0,15"/>

            <Border Grid.Row="1" Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Benchmark hệ thống:" Style="{StaticResource CardHeaderStyle}"/>
                    <Button Content="Kiểm tra căng thẳng CPU" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="CpuStressTest_Click"/>
                    <Button Content="Kiểm tra tốc độ RAM" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="RamSpeedTest_Click"/>
                    <Button Content="Kiểm tra hiệu suất đĩa" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="DiskPerformanceTest_Click"/>
                    <Button Content="Benchmark GPU" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="GpuBenchmark_Click"/>

                    <TextBlock Text="Công cụ so sánh:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <Button Content="So sánh hiệu suất" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="ComparePerformance_Click"/>
                    <Button Content="Xu hướng hiệu suất lịch sử" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="HistoricalPerformanceTrends_Click"/>
                    <Button Content="Theo dõi cải tiến" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="TrackImprovements_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>