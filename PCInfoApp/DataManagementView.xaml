<UserControl x:Class="PCInfoApp.DataManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Quản lý dữ liệu" Style="{StaticResource H2Style}" Margin="0,0,0,15"/>

            <Border Grid.Row="1" Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Tích hợp cơ sở dữ liệu:" Style="{StaticResource CardHeaderStyle}"/>
                    <TextBlock Text="Tích hợp SQLite để lưu trữ cục bộ." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Sao lưu và khôi phục dữ liệu:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Tính năng này yêu cầu cơ chế sao lưu và khôi phục tệp." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Theo dõi lịch sử hiệu suất:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Tính năng này yêu cầu lưu trữ và truy xuất dữ liệu hiệu suất lịch sử." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Quản lý tùy chọn người dùng:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Tính năng này yêu cầu cơ chế lưu trữ và tải tùy chọn người dùng." Style="{StaticResource InfoTextStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>