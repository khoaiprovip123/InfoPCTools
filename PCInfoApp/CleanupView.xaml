<UserControl x:Class="PCInfoApp.CleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Dọn dẹp đĩa" Style="{StaticResource H2Style}" Margin="0,0,0,15"/>

            <Border Grid.Row="1" Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Các loại tệp có thể dọn dẹp:" Style="{StaticResource CardHeaderStyle}"/>
                    <CheckBox x:Name="TempFilesCheckBox" Content="Tệp tạm thời" IsChecked="True" Margin="0,5,0,0"/>
                    <CheckBox x:Name="CacheFilesCheckBox" Content="Tệp bộ nhớ cache" IsChecked="True" Margin="0,5,0,0"/>
                    <CheckBox x:Name="RecycleBinCheckBox" Content="Thùng rác" IsChecked="True" Margin="0,5,0,0"/>
                    <TextBlock x:Name="CleanupStatusTextBlock" Text="" Style="{StaticResource InfoTextStyle}" Margin="0,10,0,0"/>
                    <Button Content="Bắt đầu dọn dẹp" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,20,0,0" Click="StartCleanup_Click"/>
                    <Button Content="Tối ưu hóa bộ nhớ" Style="{StaticResource SuccessColoredButtonStyle}" Margin="0,10,0,0" Click="OptimizeMemory_Click"/>
                    <Button Content="Dọn dẹp Registry" Style="{StaticResource WarningColoredButtonStyle}" Margin="0,10,0,0" Click="CleanupRegistry_Click"/>
                    <Button Content="Tìm kiếm tệp trùng lặp" Style="{StaticResource PrimaryColoredButtonStyle}" Margin="0,10,0,0" Click="FindDuplicateFiles_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>