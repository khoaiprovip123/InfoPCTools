<UserControl x:Class="PCInfoApp.DistributionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Phân phối" Style="{StaticResource H2Style}" Margin="0,0,0,15"/>

            <Border Grid.Row="1" Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="Tạo gói cài đặt:" Style="{StaticResource CardHeaderStyle}"/>
                    <TextBlock Text="Tạo trình cài đặt MSI yêu cầu các công cụ chuyên biệt (ví dụ: WiX Toolset)." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Cơ chế tự động cập nhật:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Tính năng này yêu cầu một dịch vụ cập nhật và cơ chế phân phối." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Dọn dẹp gỡ cài đặt:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Đảm bảo gỡ cài đặt sạch sẽ." Style="{StaticResource InfoTextStyle}"/>

                    <TextBlock Text="Chữ ký số:" Style="{StaticResource CardHeaderStyle}" Margin="0,20,0,0"/>
                    <TextBlock Text="Ký mã yêu cầu chứng chỉ ký mã." Style="{StaticResource InfoTextStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>