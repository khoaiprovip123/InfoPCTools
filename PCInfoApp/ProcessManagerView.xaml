<UserControl x:Class="PCInfoApp.ProcessManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PCInfoApp"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Quản lý tiến trình" Style="{StaticResource H2Style}" Margin="0,0,0,15"/>

        <TabControl Grid.Row="1" Grid.ColumnSpan="2">
            <TabItem Header="Tiến trình">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Border Style="{StaticResource InfoCardStyle}">
                                <StackPanel>
                                    <ListView x:Name="ProcessListView" SelectionChanged="ProcessListView_SelectionChanged">
                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Header="Tên tiến trình" Width="200" DisplayMemberBinding="{Binding ProcessName}"/>
                                                <GridViewColumn Header="ID" Width="80" DisplayMemberBinding="{Binding Id}"/>
                                                <GridViewColumn Header="Sử dụng CPU (%)" Width="120" DisplayMemberBinding="{Binding CpuUsage}"/>
                                                <GridViewColumn Header="Sử dụng RAM (MB)" Width="120" DisplayMemberBinding="{Binding RamUsageMB}"/>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                    <Button Content="Kết thúc tiến trình" Style="{StaticResource DangerColoredButtonStyle}" Margin="0,10,0,0" Click="KillProcess_Click"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <Border Style="{StaticResource InfoCardStyle}" Margin="10,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="Chi tiết tiến trình" Style="{StaticResource CardHeaderStyle}"/>
                                    <TextBlock x:Name="ProcessDetailsTextBlock" Style="{StaticResource InfoTextStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Chương trình khởi động">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Border Style="{StaticResource InfoCardStyle}">
                        <ListView x:Name="StartupProgramsListView">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Tên chương trình" Width="200" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="Đường dẫn" Width="300" DisplayMemberBinding="{Binding Path}"/>
                                    <GridViewColumn Header="Trạng thái" Width="100" DisplayMemberBinding="{Binding Status}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Border>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
        </UserControl>